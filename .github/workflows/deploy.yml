name: Deploy NexusAI

on:
  push:
    branches: [main]

jobs:
  notify:
    name: Notify Deployment
    runs-on: ubuntu-latest
    steps:
      - name: Deployment status
        run: |
          echo "Railway and Vercel deploy automatically from GitHub on push to main."
